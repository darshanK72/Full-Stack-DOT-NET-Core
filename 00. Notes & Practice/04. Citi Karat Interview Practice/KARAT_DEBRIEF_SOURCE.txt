# Karat / Citi debrief — raw question list (source for distribution)

Collected from interview debrief and practice notes.  
Use this file to assign questions to curriculum folders — do not treat as the final bank layout.

---

## Hosting, reverse proxy, Kestrel, IIS

- Explain Kestrel, IIS, reverse proxy roles in ASP.NET Core deployment.
- Code: ForwardedHeadersOptions + UseForwardedHeaders — app works locally HTTPS but http links and wrong client IP behind nginx. What is missing?
- How to limit incoming client requests within a time window using ASP.NET Core built-in RateLimiting middleware?

---

## Middleware & pipeline

- What is the purpose of middleware in ASP.NET Core, and when would I use it?
- Middleware short-circuiting: how/why auth middleware terminates pipeline with 401 Unauthorized.
- Review middleware / pipeline behavior (401 without calling next).

---

## Dependency injection

- Resolving multiple DI registrations: IEnumerable<T> vs single T registration.
- DI lifetime validation: ValidateScopes & ValidateOnBuild via UseDefaultServiceProvider().
- E-commerce: shopping cart stored as instance field in Singleton class — problems?
- Scoped DbContext injected into singleton hosted service — captive dependency.

---

## Web API, HTTP, serialization

- API creates resource — HTTP 201 Created: include resource and Location header how?
- JSON: why CustomerName becomes "customerName" in JSON? Control serialization naming?
- Optional boolean flag in API model/JSON — omission vs false?
- Centralised uniform exception handling in ASP.NET Core Web API.

---

## Async & concurrency (often in controllers/services)

```csharp
public IActionResult GetData() {
    var data = _service.GetAsync().Result;  // blocking
    return Ok(data);
}
```

```csharp
public async Task DoProcess() {
    var obj = await somemethod();
    await DoProcess();  // recursion
}
```

```csharp
public async Task<string> GetString(int id) {
    using var httpClient = new HttpClient();
    await httpClient.GetStringAsync("https://api.example.com/items");
}
```

---

## Code review snippets

```csharp
public class RepositoryService {
    private readonly _repository;
    public async Task<Order>(int id) {
        var obj = await _repository.Find(id);
        return new Order() { Name = obj!.Name, Id = obj!.Id };
    }
}
```

```csharp
if (dictionary.ContainsKey(key)) {
    var value = dictionary[key];
}
```

```csharp
try { /* logic */ }
catch (Exception ex) {
    _logger.LogError(ex.Message);
    throw ex;
}
```

---

## Configuration

- Role of IConfiguration and multiple providers (JSON, env, secrets, CLI) and precedence.
- Difference IOptions<T> vs IOptionsSnapshot<T> (debrief typo: Snapshort).

---

## Testing

- Unit test service calling external API without real HTTP.
- Stub vs fake vs mock — when hand-rolled fake over Moq?
- Test pyramid — WebApplicationFactory vs mocked HttpMessageHandler.

---

## Architecture / state

- static Dictionary in Singleton service — failures under load, parallel tests, multi-instance.
- Singleton cart (see DI section).

---

## Live coding (spec reading)

- Library Management System: loan statistics, tier billing, interval overlap.
- Edge cases: EndDay null vs 0, boundary-touching intervals do not overlap.
- Practice: 17. Library Management System / 1.txt / 11. Library - Done
