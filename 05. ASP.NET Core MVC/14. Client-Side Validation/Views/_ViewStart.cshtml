@* _ViewStart.cshtml
 * Runs before every view. Sets the default layout for all views in this folder tree.
 * Individual views can override: @{ Layout = "~/Views/Shared/_OtherLayout.cshtml"; }
 * or opt out entirely: @{ Layout = null; }
 *@
@{
    Layout = "_Layout";
}
