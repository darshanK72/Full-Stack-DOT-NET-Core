@* _ViewStart.cshtml — executes before every view in this folder tree.
   Sets the default Layout so each view can omit its own @{ Layout = ... } block.
   To opt out in a specific view: @{ Layout = null; }
   Scoping: applies to Views/ and all sub-folders (Home/, Products/, Shared/, etc.) *@
@{
    Layout = "_Layout";
}
